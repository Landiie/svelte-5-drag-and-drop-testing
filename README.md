testing a custom drag and drop implementation for an upcoming app where it needs to be feature-packed and tailored for the best experience possible.

Using Svelte 5 and TypeScript.

# Build

clone repo, then run `npm run dev` on the root of the repository. Navigate to the url displayed.